% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modified_kriging_rss.R
\name{estimate_s_rss_joint}
\alias{estimate_s_rss_joint}
\title{Estimate joint shrinkage parameter for two-ancestry LD-discrepancy test}
\usage{
estimate_s_rss_joint(z1, z2, R1, R2, n1 = NULL, n2 = NULL, r_tol = 1e-08)
}
\arguments{
\item{z1}{Numeric vector of z-scores from ancestry 1.}

\item{z2}{Numeric vector of z-scores from ancestry 2.}

\item{R1}{Symmetric LD correlation matrix for ancestry 1 (\eqn{p_1 \times p_1}).}

\item{R2}{Symmetric LD correlation matrix for ancestry 2 (\eqn{p_2 \times p_2}).}

\item{n1}{Integer sample size for ancestry 1. Used for finite-sample
z-score shrinkage; ignored if \code{NULL}.}

\item{n2}{Integer sample size for ancestry 2. Used for finite-sample
z-score shrinkage; ignored if \code{NULL}.}

\item{r_tol}{Numeric tolerance for flooring small negative eigenvalues to
enforce positive semi-definiteness (default \code{1e-8}).}
}
\value{
A scalar \eqn{\hat{s} \in [0,1]}: the estimated regularization
parameter.
}
\description{
Estimates a single regularization parameter \eqn{s \in [0,1]} by maximizing
the joint null log-likelihood across both ancestries simultaneously. The
regularized LD matrix is \eqn{(1-s)\mathbf{R} + s\mathbf{I}}, where
\eqn{\mathbf{R} = \mathrm{blockdiag}(R_1, R_2)}.
}
\keyword{internal}
