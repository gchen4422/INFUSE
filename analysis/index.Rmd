---
title: "INFUSE: INtegrating Functional annotations into mUlti-ancestry fine-mapping via Sum of single Effect model"
author: "Guangxin Chen"
date: "2026-04-10"
site: workflowr::wflow_site
output:
  workflowr::wflow_html:
    toc: true
editor_options:
  chunk_output_type: console
---

## INFUSE Overview

INFUSE (**IN**tegrating **F**unctional annotations into m**U**lti-ancestry fine-mapping via **S**um of single **E**ffect model) is an R package for statistical fine-mapping of causal genetic variants using GWAS summary statistics from multiple ancestry groups. INFUSE extends the SuSiE (Sum of Single Effects) framework to jointly model shared and ancestry-specific causal signals while leveraging functional genomic annotations to improve variant prioritization.

## Motivation

Standard fine-mapping methods typically operate on a single ancestry at a time and ignore the rich functional annotation data generated by large-scale genomic consortia (e.g., ENCODE, Roadmap Epigenomics). INFUSE addresses both limitations simultaneously:

1. **Multi-ancestry modeling:** By combining LD matrices and summary statistics from diverse populations, INFUSE gains power to distinguish true causal variants from those in spurious LD, and can identify variants that are causal in only a subset of ancestries.

2. **Functional annotation integration:** Incorporating per-SNP functional priors (e.g., chromatin accessibility, transcription factor binding, evolutionary conservation) shifts probability mass toward biologically plausible causal variants, improving credible set resolution.

## Statistical Framework

INFUSE solves a multi-ancestry extension of the SuSiE regression model:

$$\mathbf{z}_k = \mathbf{R}_k \boldsymbol{\gamma} + \boldsymbol{\epsilon}_k, \quad k = 1, \ldots, K$$

where $\mathbf{z}_k$ is the vector of Z-scores for ancestry $k$, $\mathbf{R}_k$ is the ancestry-specific LD matrix, and $\boldsymbol{\gamma}$ is the vector of causal effect sizes. The causal configuration is modeled as a sparse sum of $L$ single-effect vectors:

$$\boldsymbol{\gamma} = \sum_{\ell=1}^{L} \mathbf{b}_\ell$$

Each single-effect vector $\mathbf{b}_\ell$ places all its effect on exactly one SNP, with placement governed by a prior probability vector $\boldsymbol{\pi}_\ell$ that can be informed by functional annotations.

### Annotation Integration

When a SNP $\times$ feature annotation matrix $\mathbf{A}$ is provided, INFUSE learns annotation weights $\mathbf{w}$ to construct enriched priors:

$$\pi_j \propto \exp(\mathbf{A}_j^\top \mathbf{w})$$

Two annotation methods are supported:

- **`mlk`** (default): Sparse PCA on the annotation matrix reduces dimensionality before weight estimation, suitable for high-dimensional annotation sets.
- **`glm`**: A generalized linear model directly regresses annotation features against empirical PIP estimates to calibrate weights.

## Key Outputs

| Output | Description |
|:---|:---|
| `$pip` | Per-SNP Posterior Inclusion Probability for each ancestry and jointly |
| `$cs` | 95% Credible Sets — groups of SNPs containing a causal variant with 95% probability |
| `$alpha` | Posterior probability matrix ($L \times p$) for each single-effect component |
| `$lbf` | Log Bayes factors per SNP |
| `$annot_weights` | Estimated annotation weight vector $\mathbf{w}$ |

## When to Use INFUSE

INFUSE is most powerful when:

- You have GWAS summary statistics from **2 or more ancestry groups** at a locus of interest
- You have access to an **LD reference panel** matched to each ancestry
- You have **functional annotation data** for the variants in the region (optional but recommended)
- You suspect **shared causal variants** across ancestries, possibly with ancestry-specific effect modulation

> **Note:** INFUSE is intended strictly for research purposes. Results should be interpreted in the context of study design, population structure, and the quality of input data.
